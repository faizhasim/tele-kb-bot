/**
 * Shared constants for tele-kb-bot.
 *
 * Extracted to break circular dependencies between cli/ and daemon/.
 *
 * @module
 */

const VERSION = '0.1.3-SNAPSHOT-16929f9';
const BINARY_NAME = 'tele-kb-bot';

export { BINARY_NAME, VERSION };
