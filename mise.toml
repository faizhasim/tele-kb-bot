[tools]
python = "3.12"

[tasks.setup]
description = "Install project dependencies (Python tools)"
run = "uv tool install zensical"

[tasks.docs-build]
description = "Build the documentation site"
run = "zensical build --clean"

[tasks.docs-serve]
description = "Serve the documentation site locally"
run = "zensical serve"
